# Global npm packages installed by 35-node/run.sh.
#
# One package per line. Lines starting with # are comments.
# Packages already installed are skipped (idempotent).
#
# @playwright/test gets special handling: after install, browsers
# (chromium, firefox, webkit) are installed via `npx playwright install`
# — deliberately WITHOUT --with-deps: OS library deps need root and are
# owned by the root tier (init.d/06-playwright-deps).

@playwright/test

# --- Global CLI tools (from ee monorepo node setup) ---

# Check for outdated npm dependencies
npm-check-updates

# Duplicate code detection for PR reviews
jscpd

# Process manager for Node.js services
pm2

# Open agent skills ecosystem (npx skills)
skills

# MCP — Model Context Protocol tools (npx mcp)
mcp
