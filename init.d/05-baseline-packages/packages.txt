# Base packages installed on every broadminde host. The list is
# deliberately minimal — anything an individual app needs (jq, openssl,
# envsubst, wireguard, …) is installed by that app's own init step.
#
# `sudo` is here so subsequent steps can hand off to the deploy user.

# Source control + downloads
git
curl
wget

# Editors + process tools
vim
htop
unzip

# Trust anchors (apt + curl)
ca-certificates

# Privilege escalation
sudo
